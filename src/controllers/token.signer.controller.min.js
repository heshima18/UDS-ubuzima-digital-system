function addToken(o){try{const n=jwt.sign(o,process.env.SECRET_KEY);return n}catch(o){return console.log(o),null}}import dotenv from"dotenv";import jwt from"jsonwebtoken";dotenv.config();export default addToken;