import{alertMessage,getdata,getschema,postschema,request}from"../../../utils/functions.controller.js";let form=document.querySelector("form#formAuthentication"),user=form.querySelector("input#email"),subbut=form.querySelector('button[type="submit"]');form.addEventListener("submit",async e=>{e.preventDefault(),subbut.setAttribute("disabled",!0),subbut.innerText="Sending Link...",postschema.body=JSON.stringify({email:user});let t=await request("verify",postschema);subbut.removeAttribute("disabled"),subbut.innerText="Send Link",t.success,alertMessage(t.message)});